export default class Item {
  constructor(id, name, price, link, style, notes, marked) {
    this.id = id;
    this.name = name;
    this.price = price;
    this.link = link;
    this.style = style;
    this.notes = notes;
    this.marked = marked;
  }
}
